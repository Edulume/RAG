# Nginx configuration for Edulume RAG API
# /etc/nginx/sites-available/rag

upstream rag_backend {
    server 127.0.0.1:6969;
    keepalive 32;
}

server {
    listen 80;
    listen [::]:80;
    server_name rag.rahulsain.com;

    # Redirect HTTP to HTTPS (uncomment after SSL setup)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://rag_backend;
        proxy_http_version 1.1;

        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts (embedding can take time)
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 300s;

        # Buffering
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
    }

    # Health check endpoint (no logging)
    location /health {
        proxy_pass http://rag_backend/health;
        access_log off;
    }

    # Gzip compression
    gzip on;
    gzip_types application/json text/plain;
    gzip_min_length 1000;
}

# HTTPS server (uncomment after certbot setup)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name rag.rahulsain.com;
#
#     ssl_certificate /etc/letsencrypt/live/rag.rahulsain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/rag.rahulsain.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://rag_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 300s;
#     }
# }
